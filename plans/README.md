# Implementation Plans

Generated by the improve skill on 2026-06-11. Execute in the order below unless dependencies say otherwise. Each executor should read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

Planned against commit `e768767`.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix documented `hs.http` `files` array parsing | P1 | S | - | DONE |
| 002 | Redact sensitive command and URL data from runtime logs | P1 | S-M | - | DONE |
| 003 | Treat negative-monitor toast positions as visible | P1 | S | - | DONE |
| 004 | Move mouse low-level hotkeys onto a dedicated hook thread | P2 | M | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- No plan has a hard code dependency on another plan.
- Prefer executing 001 first because it is a small public API correctness fix with a clear test.
- Plan 004 has the largest native-input blast radius. Keep it separate from the smaller parser/logging/toast changes so regressions are easy to isolate.

## Deferred Findings

- Add host output and HTTP response size limits: worthwhile stability work, but it changes public script-facing options and needs a slightly larger API design pass.
- Add focused app-layer tests for native audio capture: valuable before any audio refactor, but lower urgency than the user-facing bugs above.

## Findings Considered And Rejected

- GitHub Actions major versions looked suspicious, but `actions/checkout@v6`, `actions/setup-dotnet@v5`, and `actions/upload-artifact@v7` all have upstream tags.
- `hs.keyboard.watch` callbacks that return JavaScript `undefined` looked risky in C#, but `bootstrap.js` wraps callback results with `=== true` before the host sees them.
- Startup previous-instance cleanup is intentionally broad for known HsWin process names and has focused matcher tests, so no plan was written.
- NuGet dependency audit found no vulnerable direct or transitive packages from the configured nuget.org source.
